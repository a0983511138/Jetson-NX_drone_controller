#include <deps1.h>
#include <stdio.h>

int main()
{
    int i;
    uint8_t buf[25]={0};

	sbus_decoder_get_buf(buf, 25);
    
    for(i = 0; i < 16; i++){
        printf("%d\t", rc_data.percent[i]);
    }

    return 0;
}
